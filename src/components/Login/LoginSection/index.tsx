import "./index.css"

export const LoginSection = () => {
    return(
        <section>
            <h2 className="login-title">Alredy have an account?</h2>
            <button>Login</button>
        </section>
    )
}